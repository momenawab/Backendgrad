# Django Core
Django==4.2.17
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Database
# Production uses PostgreSQL via DATABASE_URL (see psycopg2-binary below).
psycopg2-binary==2.9.10
dj-database-url==2.3.0
# mysqlclient is only needed for local MySQL (USE_MYSQL=true). It requires
# system libmysqlclient headers to build, so it is disabled by default.
# Uncomment locally if you run against MySQL:
# mysqlclient==2.2.6

# AI/ML
ultralytics==8.3.40
torch==2.5.1
torchvision==0.20.1
Pillow==11.1.0
face-recognition==1.3.0
joblib==1.3.0
scikit-learn==1.5.2
numpy==1.26.4

# WebSocket
channels[daphne]==4.2.0
channels-redis==4.2.1

# Environment
django-environ==0.12.0

# Production / deployment
whitenoise==6.8.2

# Utilities
python-dateutil==2.9.0.post0
pytz==2024.2
